import React from "react";
const LastTransactions = () => {
  return (
   <>
  
{/* <ul className="w-[100%] divide-y divide-gray-200 dark:divide-gray-700">
<div className='text-center flex-row'>
<strong className="text-gray-900 dark:text-white font-medium">Last Transactions</strong>
</div>
   {expenseCategories.map((category, index)=>(
      <li className="pb-3 sm:pb-4 mt-4" key={index}>
      <div className="flex items-center space-x-4 rtl:space-x-reverse">
      <div className={`h-8 w-8 mt-1 flex items-center justify-center ${category.color}`}>
					<category.Icon className="text-2xl text-white" />
				</div>
         <div className="flex-1 min-w-0 mt-1">
            <p className="text-sm font-medium text-gray-900 truncate dark:text-white">
               {category.name}
            </p>
            <p className="text-sm text-gray-500 truncate dark:text-gray-400">
               email@flowbite.com
            </p>
         </div>
         <div className="inline-flex items-center text-base font-semibold text-gray-900 dark:text-white">
            $320
         </div>
      </div>
   </li>

   ))}
</ul> */}

</>
  )
}

export default LastTransactions